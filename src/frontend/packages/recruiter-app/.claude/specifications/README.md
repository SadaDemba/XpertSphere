# Spécifications — recruiter-app

Un fichier Markdown par fonctionnalité spécifiée (nommage `<slug-fonctionnalite>.md`), rédigé par l'agent spec-writer avant tout développement.

Actuellement : `remove-fixed-footer-backoffice.md` (suppression complète du footer fixe du back-office, déplacement du numéro de version dans le menu compte utilisateur).
