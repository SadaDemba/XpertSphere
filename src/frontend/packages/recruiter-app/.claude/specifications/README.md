# Spécifications — recruiter-app

Un fichier Markdown par fonctionnalité spécifiée (nommage `<slug-fonctionnalite>.md`), rédigé par l'agent spec-writer avant tout développement.

Actuellement : `remove-fixed-footer-backoffice.md` (suppression complète du footer fixe du back-office, déplacement du numéro de version dans le menu compte utilisateur), `fix-experience-fields-candidate-detail-page.md` (correction de l'interface `Experience`/du template `CandidateDetailPage.vue`, dont les noms de champs ne correspondaient à aucun DTO backend réel ; documente aussi, à titre purement informatif et sans y toucher, 11 composants `.vue` orphelins reposant sur des interfaces tout aussi incorrectes mais jamais montés par aucune page — suppression volontairement écartée par décision de l'utilisateur), `fix-profile-page-update-and-logout.md` (`ProfilePage.vue` : la mise à jour de profil et la déconnexion affichaient un toast de succès alors que l'appel API réel était commenté ; corrige aussi un toast d'échec affiché en positif dans `userStore.updateUser` (partagé avec `admin/UsersPage.vue`) ; laisse `[À CONFIRMER]` le devenir du champ « Titre/Poste », sans correspondance backend), `fix-application-assign-race-condition.md` (correction d'une condition de course dans `ApplicationAssign.vue` : deux chargements concurrents de `userStore.users`, état partagé du store Pinia, pouvaient mélanger les listes « Manager » et « Évaluateur technique » ; introduction d'une méthode de store dédiée qui retourne les résultats directement sans mutation d'état partagé), `fix-job-offer-list-detail-actions.md` (4 bugs sur la gestion des offres d'emploi : actions « Dupliquer »/« Voir les candidatures » inertes depuis la liste `/jobs`, suppression sans confirmation sur la fiche détail `/jobs/:id`, champs de date vides en édition à cause d'un format ISO incompatible avec `<input type="date">`, et suffixe `€` en dur ignorant la devise réelle de l'organisation ; inclut aussi le filtrage réel de `ApplicationsPage.vue` par offre d'origine via `?jobId=`, jusque-là ignoré malgré un plumbing déjà présent côté modèle/backend), `wire-forgot-password-page.md` (câblage réel de `ForgotPasswordPage.vue` sur `POST /api/auth/forgot-password`, remplaçant le `setTimeout`/`console.log` factice actuel ; documente en tête une dépendance backend connue et non résolue — `ForgotPasswordAsync` génère un token mais n'envoie aujourd'hui aucun email réel, gap déjà noté dans `candidate-account-activation-email.md` côté backend — et une réponse backend non enumeration-safe, neutralisée côté UI par un message générique unique quel que soit le résultat réel), une spec pilotée côté backend, `role-detail-scope-fix-users-dialog-wiring.md` (`src/backend/XpertSphere.MonolithApi/.claude/specifications/`) : câblage d'une nouvelle action "Voir les utilisateurs" dans le menu de `RolesPage.vue` (le dialog associé existait déjà mais n'était jamais déclenché), et correction d'un champ de colonne incorrect (`userName` → `userFullName` dans `userRoleColumns`), `remove-public-recruiter-registration.md` (suppression pure de la page publique « Créer un compte » `RegisterPage.vue`, purement factice — aucun appel API réel —, de sa route `/auth/register`, du lien depuis `LoginPage.vue` et de l'entrée correspondante dans `authGuard.ts` ; documente en bonus, sans le traiter, l'endpoint backend orphelin `POST /auth/register` jamais appelé par aucun frontend du monorepo), et une spec pilotée côté backend, `localize-identity-error-messages.md` (`src/backend/XpertSphere.MonolithApi/.claude/specifications/`) : alignement des règles de mot de passe sur les 3 formulaires de ce package (`UsersPage.vue` création + dialog reset password, `ProfilePage.vue` changement de mot de passe) sur la politique réelle du backend, avec factorisation recommandée dans un point unique du package pour éviter toute divergence entre les 3 emplacements ; traduit aussi côté backend les messages de validation (`DataAnnotations`) de `CreateUserDto` (formulaire de création d'utilisateur, `UsersPage.vue`), avec une limite de rendu documentée (message générique, pas le détail du champ, faute de normalisation `ValidationProblemDetails` dans `userStore.createUser`) ; signale aussi, sans le traiter, un bug de câblage préexistant sur `ProfilePage.vue` (`changePassword` poste un payload `ChangePasswordDto` vers `/reset-password`, qui attend un `ResetPasswordDto`).
