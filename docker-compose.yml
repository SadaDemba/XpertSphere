services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: xpertsphere-sqlserver
    environment:
      SA_PASSWORD: "XpertSphere123!"
      ACCEPT_EULA: "Y"
      MSSQL_PID: "Developer"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql
    networks:
      - xpertsphere-network
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P XpertSphere123! -C -Q 'SELECT 1' || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 60s

  redis:
    image: redis:7-alpine
    container_name: xpertsphere-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - xpertsphere-network
    restart: unless-stopped
    command: redis-server --requirepass "XpertSphere123!"

  azurite:
    image: mcr.microsoft.com/azure-storage/azurite:latest
    container_name: xpertsphere-azurite
    command: "azurite-blob --blobHost 0.0.0.0 --blobPort 10000 --location /data --skipApiVersionCheck"
    ports:
      - "10000:10000"
    volumes:
      - azurite_data:/data
    networks:
      - xpertsphere-network
    restart: unless-stopped

  adminer:
    image: adminer:latest
    container_name: xpertsphere-adminer
    ports:
      - "8080:8080"
    networks:
      - xpertsphere-network
    restart: unless-stopped
    depends_on:
      sqlserver:
        condition: service_healthy

  monolith-api:
    build:
      context: .
      dockerfile: docker/backend/monolith-api/Dockerfile
    container_name: xpertsphere-monolith-api
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__DefaultConnection: "Server=sqlserver,1433;Database=XpertSphereDb;User Id=sa;Password=XpertSphere123!;TrustServerCertificate=true;MultipleActiveResultSets=true"
      ConnectionStrings__BlobStorage: "DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==;BlobEndpoint=http://azurite:10000/devstoreaccount1;"
      Admin__Email: ${ADMIN_EMAIL}
      Admin__Password: ${ADMIN_PASSWORD}
      Jwt__Key: ${JWT_KEY}
      CORS__ALLOWED_ORIGINS: "http://localhost:${CANDIDATE_APP_PORT:-3000},http://localhost:${RECRUITER_APP_PORT:-3001}"
      CommunicationService__BaseUrl: "http://communication-service:8080"
      CommunicationService__ApiKey: ${COMMUNICATION_API_KEY}
      Frontend__CandidateAppBaseUrl: "http://localhost:${CANDIDATE_APP_PORT:-3000}"
    ports:
      - "${API_PORT:-5050}:8080"
    depends_on:
      sqlserver:
        condition: service_healthy
      azurite:
        condition: service_started
      communication-service:
        condition: service_started
    networks:
      - xpertsphere-network
    restart: unless-stopped

  smtp4dev:
    image: rnwood/smtp4dev:v3
    container_name: xpertsphere-smtp4dev
    ports:
      - "${SMTP_PORT:-2525}:25"
      - "${SMTP_UI_PORT:-5080}:80"
    networks:
      - xpertsphere-network
    restart: unless-stopped

  communication-service:
    build:
      context: .
      dockerfile: docker/backend/communication-service/Dockerfile
    container_name: xpertsphere-communication-service
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      # SMTP defaults target the local smtp4dev catcher. Override the COMM_SMTP_*
      # variables in the (gitignored) root .env to send through a real relay
      # (e.g. Brevo) without editing this file.
      Smtp__Host: ${COMM_SMTP_HOST:-smtp4dev}
      Smtp__Port: ${COMM_SMTP_PORT:-25}
      Smtp__EnableSsl: ${COMM_SMTP_ENABLE_SSL:-false}
      Smtp__Username: ${COMM_SMTP_USERNAME:-}
      Smtp__Password: ${COMM_SMTP_PASSWORD:-}
      Smtp__From: ${COMM_SMTP_FROM:-no-reply@xpertsphere.local}
      ApiKey: ${COMMUNICATION_API_KEY}
    ports:
      - "${COMMUNICATION_API_PORT:-5051}:8080"
    depends_on:
      smtp4dev:
        condition: service_started
    networks:
      - xpertsphere-network
    restart: unless-stopped

  resume-analyzer:
    build:
      context: .
      dockerfile: docker/backend/resume-analyzer/Dockerfile
    container_name: xpertsphere-resume-analyzer
    environment:
      ENVIRONMENT: development
      LLM_PROVIDER: ${LLM_PROVIDER:-groq}
      GROQ_API_KEY: ${GROQ_API_KEY}
      GROQ_MODEL: ${GROQ_MODEL}
      GROQ_BASE_URL: ${GROQ_BASE_URL:-https://api.groq.com/openai/v1}
      GROQ_TEMPERATURE: ${GROQ_TEMPERATURE:-0.1}
      AZURE_OPENAI_ENDPOINT: ${AZURE_OPENAI_ENDPOINT}
      AZURE_OPENAI_API_KEY: ${AZURE_OPENAI_API_KEY}
      AZURE_OPENAI_API_VERSION: ${AZURE_OPENAI_API_VERSION:-2024-12-01-preview}
      AZURE_OPENAI_TEMPERATURE: ${AZURE_OPENAI_TEMPERATURE:-0.1}
    ports:
      - "${RESUME_ANALYZER_PORT:-8001}:8000"
    networks:
      - xpertsphere-network
    restart: unless-stopped

  recruiter-app:
    build:
      context: .
      dockerfile: docker/recruiter-app/Dockerfile
      args:
        VITE_APP_ENV: development
        VITE_AUTH_MODE: jwt
        VITE_WEB_API_BASE_URL: http://localhost:${API_PORT:-5050}
        VITE_APP_VERSION: docker-local
    container_name: xpertsphere-recruiter-app
    ports:
      - "${RECRUITER_APP_PORT:-3001}:3001"
    depends_on:
      monolith-api:
        condition: service_healthy
    networks:
      - xpertsphere-network
    restart: unless-stopped

  candidate-app:
    build:
      context: .
      dockerfile: docker/candidate-app/Dockerfile
      args:
        VITE_APP_ENV: development
        VITE_WEB_API_BASE_URL: http://localhost:${API_PORT:-5050}
        VITE_RESUME_ANALYZER_BASE_URL: http://localhost:${RESUME_ANALYZER_PORT:-8001}
        VITE_APP_VERSION: docker-local
    container_name: xpertsphere-candidate-app
    ports:
      - "${CANDIDATE_APP_PORT:-3000}:3000"
    depends_on:
      monolith-api:
        condition: service_healthy
      resume-analyzer:
        condition: service_healthy
    networks:
      - xpertsphere-network
    restart: unless-stopped

volumes:
  sqlserver_data:
    driver: local
  redis_data:
    driver: local
  azurite_data:
    driver: local

networks:
  xpertsphere-network:
    driver: bridge
